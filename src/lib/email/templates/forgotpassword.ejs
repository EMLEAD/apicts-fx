<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{companyName}} — Password reset code</title>
  <style>
    body{margin:0;background:#f7f8fb;font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,Arial;color:#0f172a}
    .wrap{max-width:680px;margin:28px auto;padding:20px}
    .card{background:#fff;border-radius:12px;overflow:hidden;box-shadow:0 10px 30px rgba(2,6,23,0.06)}
    .hero{background:linear-gradient(90deg,#ff7a59,#ff6b35);color:#fff;padding:22px 24px}
    .hero h1{margin:0;font-size:18px}
    .body{padding:24px;color:#334155}
    .lead{margin:0 0 12px 0}
    .otp{display:inline-block;background:#fff6f2;border:1px solid #ffe6da;color:#ff6b35;padding:14px 22px;border-radius:10px;font-weight:800;font-size:22px;letter-spacing:6px}
    .btn{display:inline-block;margin-top:18px;background:#ff6b35;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none;font-weight:700}
    .meta{margin-top:18px;font-size:13px;color:#64748b}
    .footer{background:#f1f5f9;padding:16px;text-align:center;color:#64748b;font-size:13px}
    @media (max-width:520px){.wrap{padding:12px}.otp{font-size:18px;letter-spacing:4px;padding:12px 16px}}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card" role="article" aria-labelledby="heading">
      <div class="hero">
        <h1 id="heading">Apicts-Fx — Password reset</h1>
      </div>

      <div class="body">
        <p class="lead">Hi {{userName}},</p>

        <p class="lead">Use the code below to verify your request. The code expires in {{otpTtlMinutes}} minutes.</p>

        <div style="text-align:center;margin:18px 0;">
          <div class="otp" aria-live="polite">{{OTP}}</div>
        </div>

        

        <div class="meta">
          Request details: IP {{ip}} • Device {{device}} • Time {{time}}
        </div>
      </div>

      <div class="footer">
        © {{year}} {{companyName}} — If you didn't request this, ignore this email or contact {{supportEmail}}
      </div>
    </div>
  </div>
</body>
</html>